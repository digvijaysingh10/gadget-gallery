import { Typography } from '@mui/material';
import React from 'react';

const Logo = () => {
    return (
        <div>
            <Typography variant='h4'>GadgetGallery</Typography>
        </div>
    );
}

export default Logo;
